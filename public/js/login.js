// OLD LOGIN SYSTEM DISABLED - USING UNIFIED SYSTEM IN login.html
console.log('OLD LOGIN.JS: Disabled - using unified system in login.html');

// This file is disabled to prevent conflicts with the unified login system
// The new login system is embedded directly in login.html and handles all login logic

// ORIGINAL CODE COMMENTED OUT TO PREVENT CONFLICTS:
/*
document.addEventListener('DOMContentLoaded', () => {
    // This old system was causing conflicts with the new unified login system
    // It was intercepting form submissions and preventing proper redirects
    console.log('OLD LOGIN.JS: This system is now disabled');
});
*/